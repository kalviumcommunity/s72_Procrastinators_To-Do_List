# MongoDB Atlas Connection String
# Replace the placeholders with your actual values
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/procrastinator_todo?retryWrites=true&w=majority

# To use this file:
# 1. Copy this file and rename it to .env
# 2. Replace the values with your actual MongoDB Atlas credentials
# 3. Make sure not to commit the actual .env file with your credentials to git 